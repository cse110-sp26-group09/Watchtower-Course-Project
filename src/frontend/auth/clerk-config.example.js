/**
 * Example Clerk frontend config for WatchTower.
 *
 * This file is NOT loaded by the app. It shows the shape of the generated
 * src/frontend/auth/clerk-config.js file. To configure auth locally:
 *
 *   1. Copy the repo-root .env.example to .env
 *   2. Set CLERK_PUBLISHABLE_KEY to your Clerk publishable key
 *   3. Run: npm run config:clerk
 *
 * On Render, set CLERK_PUBLISHABLE_KEY in Environment — the start script
 * generates clerk-config.js automatically.
 */

window.CLERK_PUBLISHABLE_KEY = "pk_test_REPLACE_ME";
